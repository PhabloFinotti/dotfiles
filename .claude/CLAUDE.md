@RTK.md
@nestjs-conventions.md

# Git commits

- Never mention Claude, Anthropic, or Claude Code sessions in commit messages (no `Co-Authored-By: Claude`, no `Claude-Session:` trailers, no other reference).
- Commit messages must be concise — short subject line, and a body only when it adds real context, kept brief.
